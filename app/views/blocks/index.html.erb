<h1>Listing blocks</h1>
  <%= link_to 'Toda la temporada', blocks_path %>
  <%= link_to "Día anterior", :controller => "blocks", :action => "daybefore"%>


<table>
  <tr>
    <th>Jugador</th>
    <th>Tapones</th>
    <th>Tapones por Partido</th>
  </tr>

<% @blocks.each do |block| %>
  <tr>
    <td><%= link_to  block.player.player, block.player %></td>
    <td><%= block.BLK %></td>
    <td><%= block.BLKPG %></td>
    
    
    <!--td><.%= block.id %></td>
    <td><.%= block.GP %></td>
    <td><.%= block.MPG %></td>
    <td><.%= block.BLK %></td>
    <td><.%= block.PF %></td>
    <td><.%= block.BLKPG %></td>
    <td><.%= block.BLKP48M %></td>
    <td><.%= block.BLK_PF %></td>
    <td><.%= block.day %></td>
    <td><.%= block.month %></td>
    <td><.%= block.year %></td>
    <td><.%= block.season %></td-->
    
    <!--td><.%= link_to 'Show', block %></td>
    <td><.%= link_to 'Edit', edit_block_path(block) %></td>
    <td><.%= link_to 'Destroy', block, :method => :delete, :data => { :confirm => 'Are you sure?' } %></td-->
  </tr>
<% end %>
</table>

<br />

<!--%= link_to 'New Block', new_block_path %-->
