
<nav id="menu_arriba">
        <ul>
            <li><%= link_to 'Toda la temporada', scores_path %></li>
            <li><%= link_to "Día anterior", :controller => "scores", :action => "daybefore"%></li>
            <li><%= link_to "Últimos 5 días", :controller => "scores", :action => "fivedaysbefore"%></li>
            <li><%= link_to "Últimos 10 días", :controller => "scores", :action => "tendaysbefore"%></li>
        </ul>
    </nav>      
</header>

<section id="azul">
  <div class="transazul">
    <div class="fondotransBus">

    </div>    
    <table>
      <tr>
        <th>Jugador</th>
        <th>Puntos</th>
        <th>Puntos por partido</th>    
      </tr>

    <% @scores.each do |score| %>
      <tr>
        <td><%= score.player.player %></td>
        <td><%= score.PTS %></td>
        <td><%= score.PTPG %></td>        
      </tr>
    <% end %>
    </table>
    <%= will_paginate @scores %>
  </div>
</section>